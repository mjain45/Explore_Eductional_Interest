<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hp1.ig">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activityB" />
        <activity android:name=".engineering" />
        <activity android:name=".CSIT" />
        <activity android:name=".ME" />
        <activity android:name=".EC" />
        <activity android:name=".Medical" />
        <activity android:name=".Design" />
        <activity android:name=".commerce"></activity>
    </application>

</manifest>